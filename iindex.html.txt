<!DOCTYPE html>
<html lang="ro">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>KIP Vertical Overlay</title>

<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  width: 1080px;
  height: 1920px;
  overflow: hidden;
  background: #05080f;
  font-family: Arial, sans-serif;
}

.overlay {
  position: relative;
  width: 1080px;
  height: 1920px;
  background:
    radial-gradient(circle at 75% 30%, rgba(0, 160, 255, 0.16), transparent 28%),
    radial-gradient(circle at 20% 80%, rgba(0, 255, 170, 0.10), transparent 30%),
    linear-gradient(180deg, #03060c 0%, #07101c 45%, #04070d 100%);
}

/* textură discretă */
.overlay::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.02) 1px, transparent 1px);
  background-size: 42px 42px;
  opacity: 0.35;
}

/* glow animat */
.glow {
  position: absolute;
  width: 520px;
  height: 520px;
  border-radius: 50%;
  filter: blur(90px);
  opacity: 0.5;
  animation: floatGlow 8s infinite alternate ease-in-out;
}

.g1 {
  top: 120px;
  right: -120px;
  background: #008cff;
}

.g2 {
  bottom: 120px;
  left: -150px;
  background: #00ffb3;
}

@keyframes floatGlow {
  from { transform: translateY(0) scale(1); }
  to { transform: translateY(60px) scale(1.12); }
}

/* zona sus */
.top-frame {
  position: absolute;
  left: 42px;
  top: 38px;
  width: 996px;
  height: 610px;
  border: 2px solid rgba(0, 185, 255, 0.7);
  border-radius: 22px;
  box-shadow:
    0 0 18px rgba(0, 185, 255, 0.9),
    inset 0 0 35px rgba(0, 185, 255, 0.12);
}

.logo {
  position: absolute;
  top: 24px;
  left: 50%;
  transform: translateX(-50%);
  color: #dff9ff;
  font-size: 54px;
  font-weight: 900;
  letter-spacing: 3px;
  text-shadow:
    0 0 8px #00c8ff,
    0 0 22px #006eff;
}

.logo span {
  color: #25ff72;
  text-shadow:
    0 0 8px #25ff72,
    0 0 20px #25ff72;
}

/* carduri stânga */
.left-widgets {
  position: absolute;
  top: 160px;
  left: 70px;
  display: flex;
  flex-direction: column;
  gap: 34px;
}

.widget {
  width: 125px;
  height: 115px;
  border: 1px solid rgba(0, 200, 255, 0.55);
  background: rgba(0, 10, 25, 0.55);
  box-shadow: 0 0 18px rgba(0, 200, 255, 0.25);
  border-radius: 14px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

.widget .icon {
  font-size: 34px;
  margin-bottom: 8px;
}

.widget .title {
  color: white;
  font-size: 14px;
  font-weight: 800;
}

.widget .value {
  color: #00eaff;
  font-size: 18px;
  font-weight: 900;
}

/* bara de mijloc */
.middle-bar {
  position: absolute;
  left: 42px;
  top: 680px;
  width: 996px;
  height: 118px;
  border-radius: 18px;
  background: rgba(3, 9, 18, 0.88);
  border: 1px solid rgba(0, 200, 255, 0.65);
  box-shadow:
    0 0 20px rgba(0, 200, 255, 0.65),
    inset 0 0 30px rgba(0, 200, 255, 0.12);
}

.action-text {
  position: absolute;
  width: 100%;
  top: 20px;
  text-align: center;
  font-size: 31px;
  font-weight: 900;
  color: white;
  text-shadow: 0 0 12px #00eaff;
}

.action-text span {
  color: #ff2bd6;
  text-shadow: 0 0 16px #ff2bd6;
}

.promo {
  position: absolute;
  width: 100%;
  bottom: 18px;
  text-align: center;
  color: white;
  font-size: 24px;
  font-weight: 800;
}

.promo b {
  color: #25ff72;
  font-size: 34px;
  text-shadow: 0 0 16px #25ff72;
}

/* social counters */
.kick {
  position: absolute;
  left: 70px;
  top: 722px;
  color: #25ff72;
  font-size: 36px;
  font-weight: 900;
  text-shadow: 0 0 14px #25ff72;
}

.youtube {
  position: absolute;
  right: 70px;
  top: 722px;
  color: white;
  font-size: 32px;
  font-weight: 900;
}

.youtube::before {
  content: "▶";
  color: red;
  margin-right: 10px;
}

/* goaluri dreapta */
.goals {
  position: absolute;
  right: 58px;
  top: 845px;
  width: 520px;
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.goal {
  height: 58px;
  border-radius: 12px;
  border: 2px solid rgba(0, 200, 255, 0.95);
  background: rgba(7, 18, 42, 0.85);
  box-shadow:
    0 0 14px rgba(0, 200, 255, 0.85),
    inset 0 0 22px rgba(0, 115, 255, 0.35);
  padding: 8px 18px;
  color: white;
  font-size: 18px;
}

.goal .bar {
  margin-top: 7px;
  height: 7px;
  border-radius: 20px;
  background: rgba(255,255,255,0.15);
  overflow: hidden;
}

.goal .fill {
  width: 0%;
  height: 100%;
  background: linear-gradient(90deg, #00eaff, #25ff72);
}

/* chat */
.chat-area {
  position: absolute;
  left: 42px;
  top: 990px;
  width: 996px;
  height: 870px;
  border-radius: 24px;
  border: 2px solid rgba(0, 200, 255, 0.85);
  background:
    radial-gradient(circle at 85% 85%, rgba(0, 140, 255, 0.22), transparent 28%),
    rgba(0, 5, 14, 0.42);
  box-shadow:
    0 0 22px rgba(0, 200, 255, 0.85),
    inset 0 0 40px rgba(0, 200, 255, 0.09);
}

.chat-label {
  position: absolute;
  top: -22px;
  left: 28px;
  background: #06101d;
  padding: 8px 26px;
  border: 1px solid rgba(0, 200, 255, 0.75);
  border-radius: 10px;
  color: white;
  font-size: 22px;
  font-weight: 900;
  text-shadow: 0 0 10px #00eaff;
}

/* linii clean */
.line {
  position: absolute;
  height: 2px;
  background: linear-gradient(90deg, transparent, #00eaff, transparent);
  opacity: 0.8;
  animation: scan 4s infinite linear;
}

.l1 {
  top: 650px;
  left: 100px;
  width: 880px;
}

.l2 {
  top: 955px;
  left: 80px;
  width: 900px;
}

@keyframes scan {
  0% { opacity: 0.2; transform: translateX(-30px); }
  50% { opacity: 1; }
  100% { opacity: 0.2; transform: translateX(30px); }
}
</style>
</head>

<body>
  <div class="overlay">
    <div class="glow g1"></div>
    <div class="glow g2"></div>

    <div class="top-frame"></div>

    <div class="logo"><span>KIP</span> B-ZONE</div>

    <div class="left-widgets">
      <div class="widget">
        <div class="icon">🌹</div>
        <div class="title">OBIECTIV</div>
        <div class="value">97 / 200</div>
      </div>

      <div class="widget">
        <div class="icon">🔥</div>
        <div class="title">TOP STREAK</div>
        <div class="value">x 30</div>
      </div>

      <div class="widget">
        <div class="icon">🎁</div>
        <div class="title">TIKTOK</div>
        <div class="value">500 coins</div>
      </div>
    </div>

    <div class="middle-bar">
      <div class="action-text">MONEY GUN <span>TASTA F</span></div>
      <div class="promo">PROMO CODE <b>KIP</b> LA CHECKOUT</div>
    </div>

    <div class="kick">K 0</div>
    <div class="youtube">0</div>

    <div class="goals">
      <div class="goal">
        Monede pentru cufăr : 0 / 1,500 (0%)
        <div class="bar"><div class="fill"></div></div>
      </div>

      <div class="goal">
        Like Goal : 0 / 100,000 (0%)
        <div class="bar"><div class="fill"></div></div>
      </div>
    </div>

    <div class="chat-area">
      <div class="chat-label">CHAT</div>
    </div>

    <div class="line l1"></div>
    <div class="line l2"></div>
  </div>
</body>
</html>